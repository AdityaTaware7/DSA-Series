// unique data structure
#include <iostream>
#include <set>

using namespace std;

int main(){
    set<int> s;


    // inserting elements
    s.insert(5);
    s.insert(2);
    s.insert(5);
    s.insert(8);

    //displaying elements
    

    return 0;
}